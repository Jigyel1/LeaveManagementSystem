<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="/">LM | System</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a style="color:white;"  class="nav-link" href="/">Home</a>
        </li>

      <% if current_user %>
      <% if current_user.super_admin? || current_user.admin? %>
          <li class="nav-item">
            <a class="nav-link" style="color:white;" href="/users">User Management</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" style="color:white;"  href="#">Leave Management</a>
          </li>
        <%elsif current_user.admin? || current_user.employee?%>
          <li class="nav-item">
            <a class="nav-link" style="color:white;" href="/profile/index">Profile</a>
          </li>
        <%end%>
        <% end %>

        <% if current_user %>
        <li class="nav-item dropdown">
          <a style="color:white;"  class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%if current_user.super_admin? %>
            <li><a class="dropdown-item" href="#">Account</a></li>
            <li><hr class="dropdown-divider"></li>
            <%end%>
            <li> <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
          </ul>
        </li>
        <%else%>
        <li class="nav-item">
          <a style="color:white;"  class="nav-link" href="\users\sign_in">Sign In</a>
        </li>
        <li class="nav-item">
          <a style="color:white;"  class="nav-link" href="\users\sign_up">Sign Up</a>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>